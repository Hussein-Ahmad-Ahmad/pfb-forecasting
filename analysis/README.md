# Analysis utilities

Current runnable analysis workflows are exposed through `experiments/diagnostics`
and compact outputs are stored in `results_summary/`.

Exploratory scripts remain outside the public package.
